import React from 'react';
import { Navbar } from 'react-bootstrap';

const Root = () => (
  <Navbar bg="light" expand="lg">
    <Navbar.Brand>
      Invoice App
    </Navbar.Brand>
  </Navbar>
);

export default Root;
